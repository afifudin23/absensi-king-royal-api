# common

Shared HTTP response and error utilities.

Contains:
- Generic API response schema (`success`, `data`, `error`)
- Application error model (`AppError`, `ErrorCode`)
- Error handler and validation helpers
